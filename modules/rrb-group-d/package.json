{
  "name": "rrb-group-d",
  "version": "1.0.0",
  "private": true,
  "description": "RRB Group D CBT exam module — Track Maintainer, Gateman, Pointsman, Helper posts",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/ tests/",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
